/** Session 5 durable domain facade. Keep domain imports here rather than importing the legacy Session 4 aggregate directly. */
export { canOverrideReadiness, canPublishAnnouncement, requireInstructorAssigned, requireOfferingScope, type Session4Actor } from "@/lib/session-4-permissions";
