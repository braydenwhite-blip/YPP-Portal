/**
 * Pure meeting type constants — safe to import from client components.
 * Keep this file free of prisma/server imports so `meetings-home.ts` can be
 * used in the browser without bundling the database layer.
 */
export const GLOBAL_OPERATIONS_IMPACT_MEETING_TYPE = "GLOBAL_OPERATIONS_IMPACT_PRESENTATION";
