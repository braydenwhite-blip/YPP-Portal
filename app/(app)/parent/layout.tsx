import { ParentPortalShell } from "@/components/family-portal/portal-shells";
export default function Layout({ children }: { children: React.ReactNode }) { return <ParentPortalShell>{children}</ParentPortalShell>; }
