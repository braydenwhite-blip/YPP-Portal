import { requireStudentPortalUser } from "@/lib/family-access";import { getStudentProgress } from "@/lib/session8/student-portal";import { S8Page,S8Grid,S8Card,S8List,S8Item } from "@/components/session8/portal-ui";import { shortDate } from "@/lib/session8/format";export default async function Page(){const u=await requireStudentPortalUser();const p=await getStudentProgress(u.id);return <S8Page eyebrow="Progress" title="Completed YPP participation" body="Released feedback, completion milestones, and certificates appear here without grades, rubrics, or private instructor notes."><S8Grid><S8Card title="Completed"><S8List items={p.completed} empty="No completed classes yet." render={(e:any)=><S8Item key={e.id} title={e.offering?.title??"Completed class"} meta={shortDate(e.completedAt)} status={e.status}/>} /></S8Card><S8Card title="Released feedback"><S8List items={p.feedback} empty="No released feedback yet." render={(f:any)=><S8Item key={f.id} title={f.offering?.title??"Feedback"} meta={shortDate(f.createdAt)}>{f.liked??"Feedback has been released for family viewing."}</S8Item>} /></S8Card><S8Card title="Certificates" actionHref="/student/certificates"><S8List items={p.certificates} empty="No certificates issued yet." render={(c:any)=><S8Item key={c.id} title={c.title} meta={shortDate(c.issuedAt)} status={c.template?.type}/>} /></S8Card></S8Grid></S8Page>}