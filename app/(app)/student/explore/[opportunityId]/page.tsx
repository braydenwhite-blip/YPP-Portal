import { notFound } from "next/navigation";import { requireStudentPortalUser } from "@/lib/family-access";import { evaluateFamilyEnrollment, getFamilyOpportunity } from "@/lib/family-enrollment-service";import { studentOpportunityAction } from "@/lib/family-portal-actions";const labels:any={ENROLL:"Enroll",REQUEST_GUARDIAN_APPROVAL:"Ask a guardian",APPLY:"Apply",JOIN_WAITLIST:"Join waitlist",LEAVE_WAITLIST:"Leave waitlist",ACCEPT_WAITLIST_OFFER:"Accept offer",EXPRESS_INTEREST:"Express interest"};const values:any={ENROLL:"enroll",REQUEST_GUARDIAN_APPROVAL:"approval",JOIN_WAITLIST:"waitlist",LEAVE_WAITLIST:"leaveWaitlist",ACCEPT_WAITLIST_OFFER:"acceptOffer"};
export default async function Page({params}:{params:Promise<{opportunityId:string}>}){const {opportunityId}=await params;const user=await requireStudentPortalUser();const o:any=await getFamilyOpportunity(opportunityId,user.id);if(!o)notFound();const e:any=await evaluateFamilyEnrollment(user.id,opportunityId);const next=o.sessions?.[0];return <div className="space-y-6"><header className="rounded-3xl bg-white p-6"><p className="text-sm font-semibold text-violet-700">Opportunity</p><h1 className="text-3xl font-semibold">{o.title}</h1><p className="mt-2 text-slate-600">{o.template?.description??"A YPP opportunity for students."}</p></header><section className="rounded-3xl bg-white p-5"><h2 className="text-xl font-semibold">What you will do</h2><p className="mt-2">Participate in live YPP sessions, meet an instructor, and explore the topic through discussion and practice.</p><dl className="mt-4 grid gap-2 text-sm"><div><dt className="font-semibold">Leader</dt><dd>{o.instructor?.name??"YPP instructor"}</dd></div><div><dt className="font-semibold">Schedule</dt><dd>{o.meetingDays?.join(", ")||"Schedule to be confirmed"} {o.meetingTime}</dd></div><div><dt className="font-semibold">Location</dt><dd>{o.deliveryMode==="VIRTUAL"?"Online":o.locationName??"YPP location"}</dd></div></dl></section><section className="rounded-3xl bg-white p-5"><h2 className="text-xl font-semibold">Next step</h2><p className="mt-2">{e.explanation}</p>{e.action!=="NONE"&&e.action!=="APPLY"&&<form action={studentOpportunityAction} className="mt-4"><input type="hidden" name="offeringId" value={o.id}/><input type="hidden" name="action" value={values[e.action]}/><button className="rounded-full bg-violet-700 px-4 py-2 font-semibold text-white">{labels[e.action]}</button></form>}{e.action==="APPLY"&&<a className="mt-4 inline-flex rounded-full bg-violet-700 px-4 py-2 text-white" href={e.applicationUrl}>Start application</a>}</section>{next&&<p className="rounded-3xl bg-white p-5">First upcoming session: {new Date(next.date).toLocaleDateString()} at {next.startTime}</p>}</div>}
