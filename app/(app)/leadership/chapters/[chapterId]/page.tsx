import { getLeadershipChapterOversight } from "@/lib/session-3-operations";
export default async function LeadershipChapterPage({params}:{params:Promise<{chapterId:string}>}){ const {chapterId}=await params; const lanes=await getLeadershipChapterOversight(chapterId); return <main className="p-6 space-y-4"><h1 className="text-2xl font-semibold">Where does intervention matter?</h1>{lanes.map((l:any)=><section className="rounded-2xl border p-4" key={l.stage}><h2 className="font-semibold">{l.label} ({l.count})</h2>{l.blockers.map((b:any)=><p key={b.offeringId} className="text-sm">{b.title}: {b.blocker ?? 'No blocker'} · {b.owner}</p>)}</section>)}</main>}
