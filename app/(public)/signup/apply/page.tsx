import { UnifiedApplyClient } from "@/components/signup/unified-apply-client";

export const metadata = {
  title: "Apply — YPP Pathways Portal",
};

export default function UnifiedApplyPage() {
  return <UnifiedApplyClient />;
}
